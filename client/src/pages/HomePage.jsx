const HomePage = () => {
  return (
    <section className="page">
      <header>
        <h2>Workspace Setup</h2>
        <p>Kick off a new research request.</p>
      </header>
      {/* TODO: Render topic & preference intake form */}
      {/* TODO: Persist user preferences via API once submitted */}
    </section>
  );
};

export default HomePage;
